<%= form_for(@newtest) do |f| %>
  <% if @newtest.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@newtest.errors.count, "error") %> prohibited this newtest from being saved:</h2>

      <ul>
      <% @newtest.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br />
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
